<form method="post" action="{{ path('ajout_antecedent', {'id': consultation.patient.id}) }}" class="form-sauvegarder-antecedent">
{{ form_errors(form) }}

    {{ form_row(form.type) }}
    {{ form_row(form.valeur) }}
    {{ form_row(form._token) }}

{{ form_end(form) }}