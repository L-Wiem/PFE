<form method="post" action="{{ path('modifier_description', {'id': prescription.id}) }}" class="form-sauvegarder-medicament">
{{ form_errors(form) }}

        {{ form_row(form.medicament) }}
        {{ form_row(form.quantite) }}
        {{ form_row(form.duree) }}
        {{ form_row(form._token) }}


{{ form_end(form) }}